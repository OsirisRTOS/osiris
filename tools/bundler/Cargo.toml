[package]
name = "bundler"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.47", features = ["derive"] }
elf = "0.8.0"
interface = { path = "../../interface" }
